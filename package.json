{
    "name": "linked",
    "version": "1.0.0",
    "scripts": {
        "build:regular": "browserify app/main-regular.js -t [babelify] --outfile out/bundle-regular.js",
        "build:linked": "browserify app/main-linked.js -t [babelify] --outfile out/bundle-linked.js"
    },
    "license": "MIT",
    "devDependencies": {
        "babelify": "^6.4.0",
        "browserify": "^13.0.0"
    }
}